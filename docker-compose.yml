version: "3.4"

services:
  producer-async:
    image: producer-async
    build:
      context: .
      dockerfile: Producer.Async/Dockerfile

  kafka:
    image: iskorotkov/kafka-dependent
    ports:
      - 9092

  zookeeper:
    image: zookeeper
    expose:
      - 2181
