version: "3.4"

services:
  kafkahelloworld:
    image: kafkahelloworld
    build:
      context: .
      dockerfile: kafka-helloworld/Dockerfile

  kafka:
    image: iskorotkov/kafka-dependent
    ports:
      - 9092

  zookeeper:
    image: zookeeper
    expose:
      - 2181
